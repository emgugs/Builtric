import { pageSeo } from '@/lib/site-metadata'

export const metadata = pageSeo.customQuote

export default function CustomQuoteLayout({ children }: { children: React.ReactNode }) {
  return children
}
